use this folder for uploading your file of the project.

Use the git command in comand prompt in git

1. git init
2. git add . 
 to add files
2. git commit -m "Any Message you can add."
3. git add origin URL_from git
4. git push origin to your_folder_name
